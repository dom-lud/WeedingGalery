FROM eclipse-temurin:25-jre-alpine

RUN addgroup -g 10001 appgroup \
    && adduser -D -u 10001 -G appgroup appuser \
    && mkdir -p /app/target /data/media \
    && chown -R appuser:appgroup /app /data/media

USER appuser
WORKDIR /app
